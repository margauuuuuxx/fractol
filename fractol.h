#ifndef FRACTOL_H
# define FRACTOL_H

// struct fir the imaginary and real part of complex numbers 

typedef struct s_complex {
    double real;
    double imaginary;
} t_complex;

typedef struct s_RGB {
    int r;
    int g;
    int b;
} t_RGB;

# endif 